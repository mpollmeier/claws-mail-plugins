This plugin collects various ways to notify the user of new
(and possibly unread) mail. Currently, a popup and a mail banner
are implemented.

You can configure the plugin via the Preferences - Plugins menu.

All modules can be deactivated at compile time.
